1.1.1-Release <br>
Objective -> Create the Spring boot project with Spring MVC configured into the pom.xml and connect the MySQL database: employee_management_system with the respective configurations and make the project runneable at the pair with the database.

Next Release: 1.2.1-Release <br>
Objective -> Create a EmployeeRepository to interact with Database Create custom Exceptions like ResourceNotFound and Exception handler Make "Create Employee" endpoint able to record on database Microservice. Create List Employees endpoint implementation Microservice.

