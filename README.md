1.1.1-Release
Objective -> Create the Spring boot project with Spring MVC configured into the pom.xml and connect the MySQL database: employee_management_system with the respective configurations and make the project runneable at the pair with the database.
